import org.gradle.api.initialization.resolve.RepositoriesMode

pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_PROJECT)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "PocPilotoK"
include ':app'

setBinding(new Binding([gradle: this]))
evaluate(new File(
        settingsDir,
        'fmodule/.android/include_flutter.groovy'
))

include ':fmodule'
